# 知识助手

Android 客户端应用，作为内容创作平台的移动端入口。核心能力包括录制提词视频、管理创作文稿、阅读和整理内容素材。

**知识助手**（本应用）是 **内容创作平台**（D:\code\content-creation）的用户端 App，两者共同构成"知识助手"产品体系。

## Language

**知识助手**:
产品名称。Android 客户端 App，作为内容创作平台的移动端入口。
_Avoid_: 智能提词器, teleprompter

**内容创作平台**:
知识助手配套的后端 SaaS 平台（D:\code\content-creation），提供内容采集、智能创作、录制台等服务。
_Avoid_: 后端, API 服务

**录制台**:
内容创作平台中的创作工具。输入文章和思维导图，生成多章节演示 HTML 页，每步包含口播稿、视觉主题和 TTS。
_Avoid_: RecordingDeck

**口播稿**:
录制台生成的逐步讲解文案，推送到知识助手后成为待提词的文稿内容。

**文稿**:
用户在知识助手中创建或从内容创作平台同步的提词内容，包含标题和正文。
_Avoid_: 脚本, script

**文稿管理**:
对文稿的增删改查操作。主界面展示文稿列表，每个文稿以卡片形式呈现标题、预览和时长。

**录制**:
打开摄像头拍摄视频，同时在屏幕上显示文稿并通过声文对齐自动滚动。录制产出两个核心价值：视频文件和同步提词体验。
_Avoid_: 录像, 拍摄

**录制视频**:
录制功能产出的视频文件，保存在相册"Movies/提词器"目录。

**悬浮提词**:
独立的浮动窗口模式，只显示文稿并通过声文对齐自动滚动，不涉及摄像头或视频录制。

**音频采集**:
从麦克风采集 16kHz 16-bit 单声道 PCM 原始音频数据，供语音识别使用。不保存音频文件。
_Avoid_: 录音

**语音识别（ASR）**:
将采集到的音频实时转换为文字。目前使用字节跳动豆包的 WebSocket 大模型语音识别服务。
_Avoid_: 听写

**声文对齐**:
将语音识别输出的文字与文稿进行模糊匹配，确定当前朗读位置。采用拼音相似度评分和滑动窗口算法，能容忍同音字和轻度口音。

**ASR 增量提取**:
从 ASR 每包返回的整句假设中提取新增文字的环节。豆包 ASR 每次返回当前句的完整假设，需要与上一包对比取出真正新增的部分，或在新内容与旧内容无关时整段替换。

**语音片段**:
ASR 增量提取后送入滑动窗口的最新一段文字（不含标点和空白），系统拿它去文稿里搜索匹配位置。语音片段最大长度（`windowSize`，默认 5 字）决定窗口最多容纳多少字，超出的最早文字被挤出。
_Avoid_: 定位依据, 匹配探针

**跟随锁定/锁定态**:
系统有把握地认定朗读位置，持续跟随。每次匹配得分超过匹配成功阈值且位置有推进即进入锁定态。

**跟随丢失/丢锁**:
连续多次匹配失败，系统失去对朗读位置的把握。表现为失配计数累积到最大次数后清空语音片段。

**匹配成功阈值（0.30）**:
用于判定语音片段是否在文稿中找到匹配位置的分数底线。同时满足以下两个条件时才确认朗读位置、更新当前位置并清零失配计数：

1. **最低匹配基数** — 语音片段中至少有 2 个字符的加权匹配，防止偶然的单字对齐导致位置乱跳
2. **最终得分 ≥ 0.30 且位置有推进** — 最终得分 = 拼音相似度 - 距离惩罚，且匹配到的位置比当前位置靠前

单字匹配分三档：
- **同音匹配**（+1.0）：拼音完全相同。容忍同音别字（在/再、的/得/地、做/作）
- **声母匹配**（+0.2）：仅声母相同（zhang/chang、zhan/zan）
- **不匹配**（+0.0）：完全无关

距离惩罚：前进 ≤2 字不罚，3-8 字线性递增 0.015~0.09，>8 字陡峭递增至多 0.60

**匹配失败清空语音片段次数（3）**:
连续匹配得分低于匹配成功阈值达到此次数时，自动清空语音片段，系统从当前位置重新开始匹配。避免累积无效文字导致后续匹配持续失败。

**搜索范围**:
从当前朗读位置往前搜索匹配的区间。可配置两个参数：
- **前向搜索字数**（默认 2 行对应的字数） — 从当前行往后搜索不超过此字数寻找匹配。短行（≤6 有效字）时自动翻倍，防止短行中搜不到足够位置
- **后退搜索字数**（默认 3 字） — 从当前位置往回退多少个字开始搜索

**匹配位置**:
去除标点和空白后的纯字符序列上的位置，是滑动匹配实际操作的层面。引擎初始化时对原始文稿做一次预处理：去标点、生成拼音序列、建立位置映射表，后续每次匹配直接复用，不做重算。匹配完成后通过位置映射转换为文稿位置去驱动滚动。

**文稿位置**:
原始文稿（含标点和换行符）中的字符位置，用于驱动滚动和朗读高亮的显示。

**位置映射**:
匹配位置索引与文稿位置索引之间的转换关系。标点和空白在匹配面中被去除，导致两个层面索引不一致，需要通过预计算的位置映射表进行转换。

**滑动匹配**:
拿语音片段的拼音序列，在搜索范围内从前往后逐位滑动，每到一位计算一次最终得分（拼音相似度 - 距离惩罚），最后取得分最高的位置作为匹配结果。

**自动滚动**:
根据声文对齐确定的朗读位置，平滑地将文稿滚动到对应行。使用 SpringAnimation 物理动画实现自然流畅的滚动效果。

**蓝牙翻页**:
通过蓝牙遥控器按键（音量键、拍照键、播放键等）手动触发文稿滚动，每次滚动一行。是声文对齐自动滚动的辅助手段，用于在自动定位不准时手动修正朗读位置。按键有 300ms 防抖。

**朗读高亮**:
自动滚动时，已读部分显示为金色 (#FFD700)，当前正在朗读的词显示为白色背景。这是声文对齐结果的可视化反馈。

**录制调试行**:
录制页顶部显示的调试信息：ASR 识别预览、声文对齐结果（匹配成败、语音片段、得分）。用于观察识别与对齐状态。
_Avoid_: ASR 显示, 识别文字
